# データバックアップ・復元機能 仕様書

**作成日**: 2025-11-18 00:44
**ステータス**: 採用決定（必須機能）
**優先度**: 高（Phase 1で実装）

---

## 📋 概要

麻雀アプリのデータを外部ファイルにエクスポート（バックアップ）し、必要に応じてインポート（復元）できる機能。App Store公開に向けて、データロス対策として必須。

---

## 🎯 目的

1. **データロス防止**: 端末故障・紛失時のデータ保護
2. **機種変更対応**: 新しい端末へのデータ移行
3. **データ管理**: 定期的なバックアップによる安心感
4. **他アプリ移行**: CSV形式でのデータポータビリティ

---

## 📊 実装範囲

### Phase 1（必須・優先実装）

#### 1. JSONエクスポート
- 全データを単一JSONファイルに出力
- ファイル名: `mahjong-backup-YYYYMMDD-HHMM.json`
- 形式:
```json
{
  "version": "1.0.0",
  "exportDate": "2025-11-18T00:44:00Z",
  "appVersion": "0.1.0",
  "users": [...],
  "sessions": [...],
  "settings": {...}
}
```

#### 2. JSONインポート
- JSONファイルからデータを復元
- データ検証（バージョン、スキーマ）
- マージ方法選択:
  - **上書きモード**: 既存データ削除 → 新規データ投入
  - **追加モード**: 既存データ保持 → 重複チェックして追加
  - **差分モード**: 新しいデータのみ追加

#### 3. データ検証・エラーハンドリング
- JSONスキーマ検証
- バージョン互換性チェック
- 破損データの検出
- ロールバック機能（失敗時は元の状態に戻す）

### Phase 2（推奨）

#### 4. CSVエクスポート
- 複数ファイル生成:
  - `sessions.csv`: セッション一覧
  - `hanchans.csv`: 半荘詳細
  - `player-results.csv`: プレイヤー結果
  - `users.csv`: ユーザー一覧

#### 5. Capacitor Filesystem統合
- iOS/Android のDocuments/Downloadsフォルダに保存
- 「共有」メニューから他アプリへ送信可能
- ファイルピッカーでインポート

#### 6. 自動バックアップ
- 週次/月次の自動バックアップ設定
- ローカルストレージに保存
- 最新N世代を保持

### Phase 3（将来構想）

#### 7. iCloud/Google Driveバックアップ
- クラウドストレージ連携
- デバイス間同期
- 自動バックアップ

---

## 🎨 UI設計

### 配置場所
**設定タブ > データ管理セクション**

```
┌─────────────────────────┐
│ 設定                     │
├─────────────────────────┤
│ ...（既存設定）          │
├─────────────────────────┤
│ データ管理               │
│                          │
│ [エクスポート（JSON）]   │
│ [エクスポート（CSV）]    │
│ [インポート]             │
│ [全データ削除]           │
│                          │
│ 最終バックアップ:        │
│ 2025-11-17 20:30        │
└─────────────────────────┘
```

### インポートフロー

1. **ファイル選択**
   - ファイルピッカー表示
   - `.json`ファイルのみフィルタ

2. **データプレビュー**
   ```
   ┌─────────────────────────┐
   │ インポート確認           │
   ├─────────────────────────┤
   │ ファイル名:              │
   │ mahjong-backup-...json  │
   │                          │
   │ 内容:                    │
   │ ・ユーザー: 5名         │
   │ ・セッション: 24件      │
   │ ・半荘: 72件            │
   │                          │
   │ エクスポート日時:        │
   │ 2025-11-17 20:30        │
   ├─────────────────────────┤
   │ マージ方法:              │
   │ ○ 上書き（既存削除）    │
   │ ○ 追加（重複チェック）  │
   │ ○ 差分（新規のみ）      │
   ├─────────────────────────┤
   │ [キャンセル] [実行]     │
   └─────────────────────────┘
   ```

3. **実行確認**
   - 「上書き」の場合は再確認ダイアログ
   - 「既存のデータは削除されます。よろしいですか？」

4. **処理実行**
   - プログレス表示
   - エラー発生時はロールバック

5. **結果表示**
   - 成功/失敗メッセージ
   - インポート件数（ユーザー、セッション、半荘）

---

## 🔧 技術仕様

### データ構造（JSON）

```typescript
interface BackupData {
  version: string;           // バックアップフォーマットバージョン
  exportDate: string;        // ISO 8601形式
  appVersion: string;        // アプリバージョン
  users: User[];
  sessions: Session[];
  hanchans: Hanchan[];
  playerResults: PlayerResult[];
  settings: AppSettings;     // 将来的にグローバル設定を追加
}
```

### バージョン管理

- **フォーマットバージョン**: `1.0.0`（セマンティックバージョニング）
- **互換性ルール**:
  - メジャーバージョン違い: インポート不可（エラー）
  - マイナーバージョン違い: 警告表示 + 互換レイヤーで変換
  - パッチバージョン違い: 警告なしでインポート

### データ検証

```typescript
// スキーマ検証
function validateBackupData(data: unknown): data is BackupData {
  // バージョンチェック
  if (!data.version || !isCompatibleVersion(data.version)) {
    throw new ValidationError('Incompatible backup version');
  }

  // 必須フィールドチェック
  if (!Array.isArray(data.users)) {
    throw new ValidationError('Invalid users data');
  }

  // データ整合性チェック
  validateDataIntegrity(data);

  return true;
}
```

### エラーハンドリング

```typescript
try {
  // トランザクション開始
  await db.transaction('rw', db.users, db.sessions, async () => {
    // 既存データのバックアップ（ロールバック用）
    const backup = await createInMemoryBackup();

    try {
      // データインポート処理
      await importUsers(data.users);
      await importSessions(data.sessions);
      // ...
    } catch (error) {
      // エラー発生時はロールバック
      await restoreFromBackup(backup);
      throw error;
    }
  });

  // 成功
  showSuccessToast('データをインポートしました');
} catch (error) {
  logger.error('Import failed', { error });
  showErrorToast('インポートに失敗しました');
}
```

---

## 📱 実装ファイル

### 新規作成

1. **`src/lib/backup-utils.ts`** (200-300行)
   - `exportToJSON()`
   - `importFromJSON()`
   - `validateBackupData()`
   - `createInMemoryBackup()`
   - `restoreFromBackup()`

2. **`src/components/BackupSection.tsx`** (150-200行)
   - エクスポートボタン
   - インポートボタン
   - 最終バックアップ日時表示

3. **`src/components/ImportDialog.tsx`** (200-250行)
   - ファイル選択
   - データプレビュー
   - マージ方法選択
   - 実行・キャンセル

### 修正

4. **`src/components/tabs/SettingsTab.tsx`**
   - `<BackupSection />` 追加

---

## ✅ テスト計画

### ユニットテスト

1. **backup-utils.ts**
   - `exportToJSON()`: 全データが正しく出力されるか
   - `importFromJSON()`: 各マージモードが正しく動作するか
   - `validateBackupData()`: 不正なデータを検出できるか

### E2Eテスト（Playwright）

1. **エクスポート機能**
   - 設定タブ > データ管理 > エクスポート
   - ファイルがダウンロードされるか
   - JSON形式が正しいか

2. **インポート機能**
   - ファイル選択 → プレビュー → 実行
   - 上書きモード動作確認
   - 追加モード動作確認
   - エラーハンドリング確認

---

## 🚧 実装上の注意点

### 1. Capacitor統合（Phase 2）

```typescript
import { Filesystem, Directory } from '@capacitor/filesystem';

// ファイル保存
async function saveBackupFile(data: string, filename: string) {
  await Filesystem.writeFile({
    path: filename,
    data: data,
    directory: Directory.Documents,
    encoding: Encoding.UTF8
  });
}

// ファイル読み込み
async function loadBackupFile(filename: string) {
  const result = await Filesystem.readFile({
    path: filename,
    directory: Directory.Documents,
    encoding: Encoding.UTF8
  });
  return result.data;
}
```

### 2. ファイルサイズ対策

- 大量データの場合、JSON圧縮（gzip）を検討
- ストリーミング処理（chunk分割）

### 3. プライバシー配慮

- バックアップファイルには個人情報（ユーザー名）が含まれる
- ファイル共有時の注意喚起

---

## 📈 成功指標

- エクスポート成功率: 100%
- インポート成功率: 99%以上（不正ファイルは失敗OK）
- データ整合性: インポート後のデータが元データと完全一致
- ユーザー満足度: App Store公開時の必須機能として評価

---

## 🔗 関連ドキュメント

- `CLAUDE.md`: プロジェクト概要
- `project-docs/2025-10-03-initial-discussion/09-DATA_MODEL_DESIGN.md`: データモデル詳細
- `src/lib/db.ts`: Dexieスキーマ定義

---

## 📝 更新履歴

- **2025-11-18 00:44**: 初版作成（Phase 1-3の仕様策定）
